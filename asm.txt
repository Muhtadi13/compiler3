.MODEL SMALL
.STACK 100H

.DATA
 DW c INT
 DW a INT
 DW b INT
.CODE

haha PROC

	PUSH BP
	MOV BP , SP

	SUB SP , 4
L2 :
	PUSH CX
	PUSH BP
	MOV BX, 2
	ADD BP, BX
	POP AX
	POP CX
	MOV [BP], CX
	MOV BP, AX
L1 :
L3 :

	MOV SP , BP
	POP BP
	RET 2

haha ENDP

main PROC

	MOV AX , @DATA
	MOV DS , AX

	PUSH BP
	MOV BP , SP

	SUB SP , 26
L7 :

	SUB SP , 2
L6 :
	PUSH CX
	PUSH BP
	MOV BX, 4
	ADD BP, BX
	POP AX
	POP CX
	MOV [BP], CX
	MOV BP, AX
L5 :
	PUSH CX
	PUSH BP
	MOV BX, 2
	ADD BP, BX
	POP AX
	POP CX
	MOV [BP], CX
	MOV BP, AX
L4 :
L8 :

	MOV SP , BP
	POP BP

	MOV AH , 4CH
	INT 21H

MAIN ENDP
